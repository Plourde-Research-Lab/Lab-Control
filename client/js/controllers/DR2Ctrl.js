angular.module('labControlApp').controller('DR2Ctrl', [
    '$scope', '$http',
    function($scope, $http) {
        $scope.model = {
            message: 'This is the controller for dealing with DR2.'
        };
    }
]);